//Very simple, given a number, find its opposite.

function opposite(number) {
	if (number > 0 ){
		return -number
		}else {
	   return Math.abs(number)   // Math.abs conversts negative numbers into posative
	}
   }

   function opposite(number) {
	return -1 * number; 
   }
